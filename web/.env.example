# 食在交大 管理后台（Web）环境变量占位
# 用法：复制本文件为 .env.local 并填入真实值（.env.local 不入库）。

# ---------- 后端 API 基础路径 ----------
# 本地联调：http://127.0.0.1:8080/api
# 连云上：https://<你的云托管域名>/api
VITE_API_BASE_URL=http://127.0.0.1:8080/api

# ---------- 管理端口令（2026-09-13 定型：后台无登录体系）----------
# 必须与后端环境变量 ADMIN_TOKEN 完全一致；请求由 api/http.ts 以 X-Admin-Token 头自动携带，用户无感。
# 后台仅在你本机运行，切勿把真实口令提交到仓库。
VITE_ADMIN_TOKEN=your-admin-token
