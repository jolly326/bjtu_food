# ============================================================
# 开发环境配置 - 覆盖 application.yml 中的同名配置
# ============================================================

spring:
  datasource:
    # 开发环境使用本地 MySQL
    url: jdbc:mysql://localhost:3306/bjtu_food?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: root

mybatis-plus:
  configuration:
    # 开发环境打印完整 SQL 日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  # 开发环境使用固定密钥，方便调试
  secret: dev-secret-key-bjtu-food-2024
  expiration: 604800000

upload:
  path: ./uploads
