<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjtufood.favorite.mapper.FavoriteMapper">

    <!-- DishVO 结果映射 -->
    <resultMap id="favoriteDishMap" type="com.bjtufood.dish.dto.DishVO">
        <id property="id" column="id"/>
    </resultMap>

    <!-- 查询用户收藏的菜品列表 -->
    <select id="selectFavoriteDishes" resultMap="favoriteDishMap">
        SELECT
            d.id,
            d.name,
            d.price,
            d.images  AS images_json,
            d.tags,
            d.description,
            d.avg_rating,
            d.rating_count,
            d.favorite_count AS collect_count,
            d.view_count,
            s.name AS stall_name,
            c.name AS canteen_name
        FROM favorite f
                 JOIN dish d ON f.dish_id = d.id
                 JOIN stall s ON d.stall_id = s.id
                 JOIN canteen c ON s.canteen_id = c.id
        WHERE f.user_id = #{userId}
          AND d.status = 'on'
          AND s.status = 'open'
          AND c.status = 'open'
        ORDER BY f.created_at DESC
    </select>

</mapper>
