# Loop 状态看板（STATUS）

- **当前轮次**：ITER-002（进行中，起点 2026-09-14 用户醒来后的第二轮对齐）
- **上一轮结论**：ITER-001 三端构建全绿 + 清理管理端残留链；停止条件 S2
- **本轮主题**：蓝图二轮对齐（实现前对齐，防返工）
- **问题渠道**：`docs/loop/QUESTIONS.md`

## ITER-002 已拍板（2026-09-14 用户决策）
| 编号 | 问题 | 决策 | 开发量 |
|---|---|---|---|
| Q-009 | 管理端新增菜品落 `pending` → 小程序不显示 | 管理员即权威：新增/编辑直接 `approved` | 已实现（3 行） |
| Q-010 | 学生投稿表单形态 | 沿用现有反馈表单：自由文本 + 图，管理员人工解析 | 零开发（仅文案） |
| Q-011 | 「有用」点赞是否双约束 | 仅需登录，不适用 §7.5 双约束 | 零开发（确认现状） |
| Q-012 | 举报/反馈处理是否通知提交人 | 通知提交人 + 承诺 48h 内处理 | 文案（通知机制已有） |

## 候选池

### P0（阻塞上线，均等用户）
1. Q-004 重新部署云托管使 `ADMIN_TOKEN` 生效 → 后台恢复可用
2. Q-005 后台上传 31 道菜首图（注意：Q-009 已修，部署后新增菜品即刻可见）
3. Q-006 小程序 downloadFile 合法域名加 COS 域名

### P1（正确性/体验）
4. 部署后复测：`/admin/dishes` 200 + COS 上传指纹 + 新增菜品 `audit_status=approved`
5. §7.7 上线前必做：无 openid 账号发评价的 403 前端引导文案
6. §7.7 上线前必做：反馈投稿类目文案「提交后由管理员审核后上架」
7. §7.8：反馈/举报说明文案体现「48 小时内处理」

### P2（优化）
8. Q-007 反馈「一键转菜品」（二期）
9. Q-008「喜欢/有用」计数口径定型与注释清理
10. 占位文件物理删除（`web/src/api/admin.ts`、`stores/adminUserStore.ts`、`views/admin/AdminManageView.vue`）——需用户批准删除
11. 图片占位兜底策略（若实拍图补不齐）

## 待用户答案（ITER-002 待确认清单）
见 `docs/loop/QUESTIONS.md` 的 Q-013 ~ Q-018（功能边界、暂不实现范围、异常边界、数据来源、验收标准、冲突依赖）

## 循环触发
- 手动：用户说「继续下一轮」
- 自动（可选）：`FREQ=DAILY;BYHOUR=2;BYMINUTE=0`
