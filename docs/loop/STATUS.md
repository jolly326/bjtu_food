# Loop 状态看板（STATUS）

- **当前轮次**：ITER-001（已结束）
- **上一轮结论**：三端构建全绿；清理掉「管理端打开即请求已下线接口」的残留链；剩余 P0 全部为用户侧动作
- **停止条件判定**：**S2（无米下锅）** —— 代码侧可达的 P0/P1 已清零，剩余项均需用户在线上环境操作
- **问题渠道**：`docs/loop/QUESTIONS.md`（开放 5 条：Q-004~Q-008）

## 候选池（下一轮起点）

### P0（阻塞上线，均等用户）
1. Q-004 重新部署云托管使 `ADMIN_TOKEN` 生效 → 后台恢复可用
2. Q-005 后台上传 31 道菜首图 → 小程序瀑布流不再空图
3. Q-006 小程序 downloadFile 合法域名加 COS 域名 → 正式版图片可见

### P1（正确性/体验）
4. 待 Q-004 生效后：线上复测「带 X-Admin-Token 的 `/admin/dishes` 应 200」与「COS 上传生效指纹」
5. 小程序端无 openid 账号发评价时的 403 引导文案（§7.7 上线前必做第 1 条的前端侧）
6. 反馈入口投稿类目文案「提交后由管理员审核后上架」（§7.7 上线前必做第 2 条）

### P2（优化）
7. Q-007 反馈「一键转菜品」（§7.3 二期）
8. Q-008「喜欢/有用」计数口径定型与注释清理
9. 占位文件物理删除（`web/src/api/admin.ts`、`stores/adminUserStore.ts`、`views/admin/AdminManageView.vue`）——需用户批准删除操作
10. 生成图片占位兜底策略（若用户短期内补不齐 31 张实拍图）

## 阻塞项（等用户）
| 事项 | 阻塞什么 | 需要用户做什么 |
|---|---|---|
| Q-004 | 后台全部功能 | 云托管控制台确认变量并**重新部署** |
| Q-005 | 小程序菜品图 | 后台逐条上传首图 |
| Q-006 | 正式版图片显示 | 微信后台加 downloadFile 域名 |
| 占位文件删除 | 代码洁净度 | 允许删除文件的操作权限 |

## 循环触发
- 手动：用户说「继续下一轮」
- 自动（可选）：`FREQ=DAILY;BYHOUR=2;BYMINUTE=0` 定时任务，prompt 指向 `docs/PRODUCT_LOOP.md`
