# 审计报告 · 质量门禁 · 2026-09-14

> 范围：三端（`server/` `client/` `web/`）**全量扫描**，为「业务原则 + 产品蓝图」提供客观事实底数。只报**可复现的事实**，不提产品建议。
> 前置（已读）：`docs/project_spec.md`（§0.3/§0.5 红线、§2 目录与组件组织、§3 接口规范、§4.9 UI 红线、§5.x 三端一致性红线）、`docs/api-design.md`。
> 去重基线（已读，**本文不重复其问题**）：`docs/loop/audit/2026-09-14-tech-lead.md`（TL-01~15）、`2026-09-14-backend.md`（BE-01~15）、`2026-09-14-client.md`（CL-01~15）、`2026-09-14-uiux.md`（UX-01~12）、`2026-09-14-web.md`（WEB-01~15）。本文聚焦 **门禁视角**（构建结果 / 红线 grep 计数 / 孤儿资产 / TODO / 冗余量化 / 规模指标）。
> 只读审计，未改动任何代码与文档（除本报告）。
>
> **门禁构建结果（本机实测）**：`server` `mvn -q clean compile -DskipTests` → **EXIT=0**（BUILD SUCCESS）；`client` `npm run type-check`（vue-tsc --noEmit）→ **EXIT=0**；`client` `npm run build:mp-weixin` → **Build complete, EXIT=0**（产出 `dist/build/mp-weixin/app.js` 存在）；`web` `npm run build`（含 vue-tsc）→ **built in 3.62s, EXIT=0**。三端构建门禁**全绿**。

| 编号 | 分类 | 位置(file:line) | 现象 | 证据(命令/计数) | 影响 | 建议 | 成本(S/M/L) |
|---|---|---|---|---|---|---|---|
| QA-01 | 红线违背 | `client/uni.scss`（全文件，尤其 `:13 --color-primary: #7A241A`；`client/src/theme/tokens.ts:4 primary:'#C45549'`；`client/src/App.vue:31 --color-primary: #C45549`） | **存在第二套 design token 真源**：`uni.scss` 声明 `--color-primary:#7A241A`（陈旧朱砂红）等全套 token，与事实源 `tokens.ts` / `App.vue`（`#C45549`）**色值冲突**。uni-app 编译期会把项目根 `uni.scss` 自动注入每个组件 style 作用域（`vite.config.ts` 未禁用该行为）。spec §4.2 亦自认该文件「陈旧且与事实源冲突，待清理」。 | `Select-String -Path client/uni.scss -Pattern '^\s*--([a-z0-9-]+):'` 提取 41 个 token 键，其中 **13 个键仅存在于 `uni.scss`、`App.vue` 无对应声明**（`color-primary-dark`/`color-star`/`color-success`/`text-quaternary`/`text-white-secondary`/`bg-glass`/`bg-glass-dark`/`blur-material`/`font-card`/`font-headline`/`font-h1`/`icon-sm`/`icon-lg`）。消费计数：13 个独有键中仅 `--color-star` 命中 3 处，但逐一核对为 `--color-star-empty` 的子串误配（`DishSummaryCard.vue:19,75` + `App.vue:49`），**真实独有键消费数 = 0**。 | 双 token 真源违反 spec §4.2「色值为全站唯一事实源」。当前因 `App.vue page{}` 优先级更高 + 13 个独有键零消费，**尚未造成可见色差**，但后续任何在组件内引用这 13 个键之一者会静默取到陈旧值；改主色时存在漏改风险。 | 删除 `client/uni.scss`（或清空为注释说明），13 个独有键如确认无消费则一并移除；确认 `--color-star` 实际消费点后回归 `App.vue`/`tokens.ts`。**注意**：此条为 spec §4.2 已登记但未执行的清理项，属待办未闭环。 | S |
| QA-02 | 红线违背 | `client/src/pages.json:88-89` | `globalStyle` 中 `"navigationBarBackgroundColor": "#F5F5F7"`、`"backgroundColor": "#F5F5F7"` 两处裸 hex，未走语义 token；且 `#F5F5F7` 与 `App.vue` 的 `--nav-bar-background`（`tokens.ts:73 NAVIGATION_BAR_BACKGROUND = '#F5F5F7'`）为同值但未引用注册常量。 | `Select-String -Path client/src/pages.json -Pattern '#[0-9a-fA-F]{3,8}'` → 2 处（第 88、89 行）。 | `pages.json` 是 JSON，无法用 `var()`，但 spec §4.9 要求「原生 API 不接受 var() 的颜色例外**必须集中登记**」。此 2 处已存在于 `tokens.ts` 常量（`NAVIGATION_BAR_BACKGROUND`），却以字面量重复书写，**改色时不会同步**，属未登记裸 hex。 | 在 `tokens.ts` 注释登记 `pages.json` 两处消费点（与现有 `NAVIGATION_BAR_BACKGROUND` 关联），或将 `pages.json` 生成步骤纳入常量引用，保证全局改色可同步。 | S |
| QA-03 | 孤儿资产 | `client/src/api/category.ts:14`（`getCategories`） | `api/category.ts` 整模块**零引用**：无任何文件 `import ... from '@/api/category'`，`getCategories()` 无调用方。 | `Get-ChildItem client/src -Recurse -Include *.vue,*.ts | Select-String -Pattern "api/category"` → **0 命中**；全仓 `getCategories` → 仅模块自身定义处命中。 | 后端 `GET /categories`（`CategoryController.java:38`）在**小程序端零消费**（首页品类滚轮已在历史迭代移除，页面注册 9 页中无品类消费页）。死接口方法 + 死模块，增大维护面与误用面。 | 与 `2026-09-14-client.md` CL-02 为同一事实（CL-02 已登记为「接口未接通」）；QA 视角补充：应判定为**孤儿模块**（非仅未接通），确认无规划后整体删除。 | S |
| QA-04 | 孤儿资产 | `client/src/types/related-item.ts:1`（`RelatedItem`） | 整个类型文件零引用：`interface RelatedItem { id; name; image; type:'dish'|'stall' }` 无任何 import 方。 | `Get-ChildItem client/src -Recurse -Include *.vue,*.ts | Select-String -Pattern 'related-item|RelatedItem'` → 仅 `types/related-item.ts:1` 自身命中；用户端 `relatedType` 使用处（`useFeedback.ts:488-537`、`useReport.ts:53`）**均未引用该类型**（各自用裸字符串）。 | 孤儿类型定义（疑似随社区/关联对象链路下线后残留），与 spec §0.5「下线须清理干净」口径不符。 | 删除 `client/src/types/related-item.ts`；如后续确实需要关联对象类型，应在使用处显式引入而非留孤儿。 | S |
| QA-05 | 孤儿资产 | `client/src/components/IconSvg.vue:28,31,35`（`heart-filled`/`thumb-filled`/`send-simple`） | ICONS 注册表中 3 个图标键**零消费**：`heart-filled`、`thumb-filled`、`send-simple`。 | 逐一反查（`Get-ChildItem client/src -Recurse -Include *.vue,*.ts | Select-String -Pattern 'heart-filled'`）→ 仅 `IconSvg.vue` 定义处命中，全仓无 `<IconSvg name="...">` 或动态键引用；`thumb-filled`、`send-simple` 同。注：`home-filled`/`profile-filled` 经 `TabBar.vue:15` 的模板字面量 `` `${item.icon}-filled` `` 动态消费，**不算孤儿**。 | 死图标资产。其中 `heart`/`heart-filled` 与 spec §5.x「收藏全量移除、语义仅保留 ic-heart=喜欢」的措辞残留相关：`heart` 键（`IconSvg.vue:27`）亦零消费——**图标层面仍有「喜欢/心形」语义资产，但端上无任何入口消费**。 | 删除 3 个零消费键；`heart`/`heart-filled` 的存废须与 spec §5.x「喜欢/收藏单一概念」口径对齐后决定（含「无收藏图标」要求的执行证据）。 | S |
| QA-06 | 死代码与幽灵注释 | `server/src/main/java/com/bjtufood/canteen/service/impl/CanteenServiceImpl.java:143` | 注释写作「主要菜品（评分前3）与菜品数（**task todo#3**：档口卡展示）」——引用的 `task todo#3` 在 `tasks/` 中已不可追溯（无可对应的在册 task 编号），属幽灵引用；紧邻代码 (`:144-145`) 实际行为是 `setDishCount` + `setTopDishes`，与注释前半句一致、与「todo#3」引用无关。 | `Get-ChildItem server/src -Recurse -Include *.java | Select-String -Pattern 'TODO|FIXME|task todo'` → 该行为后端**唯一**的 `todo` 字面量命中（其余全仓命中均为 `console.log`/UUID 正则等误配）。 | 幽灵注释使后人以为存在未完成的 `task#3` 待办，实际无对应任务条目。 | 删除「task todo#3：」引用，保留行为描述。 | S |
| QA-07 | 死代码与幽灵注释 | `server/src/main/java/com/bjtufood/dish/controller/admin/StatsController.java:44-52`（`@RestController`）+ `:58`（`overview` 无映射注解） | `StatsController` 标注 `@RestController`，但**类内无任何 `@RequestMapping/@GetMapping`，`overview(int)` 亦无映射注解** → 该 Bean **不暴露任何 HTTP 端点**，仅被 `DashboardController.java:23,35` 作为普通 Bean 注入调用，实质是「Service 伪装成 Controller」。 | `Get-ChildItem server/src/main/java -Recurse -Filter *Controller.java | Select-String -Pattern '@(RequestMapping|GetMapping|PostMapping|PutMapping|DeleteMapping)'` → `StatsController.java` **0 命中**（18 个 Controller 中唯一 0 映射者）；`Select-String -Pattern 'StatsController'` → 仅 `DashboardController` 注入。 | 违反 spec §2「后端按业务分包、每模块 controller/service 四层、禁止跨层调用」的分层本意：控制器类承载业务逻辑（注入 7 个 Mapper，已于 TL-07 登记架构债），且以 `@RestController` 误导读者以为存在 `/admin/stats` 端点（`api-design.md:180` 确有 `GET /admin/stats/**` 记载，属 **api-design 文档幽灵条目**）。 | 将 `StatsController` 更名为 `StatsService`（或下沉至 service 层）并去除 `@RestController`；同步删除 `api-design.md:180` 的 `GET /admin/stats/**` 记载。**注意**：TL-07 已登记其 7-Mapper 注入与 `DashboardController` 依赖为该类的架构债，本条**补充**「`@RestController` 零映射 + 文档幽灵条目」这一独立事实。 | M |
| QA-08 | TODO | 全仓（`client/src` + `web/src` + `server/src`） | **真实 `TODO`/`FIXME`/`XXX` 待办为零**。全仓 grep 命中 8 处，**逐一核对均为误配或有效说明**：(a) `AppButton.vue:40`「emit 参数会丢失（同 DishCard 坑，见其注释）」——有效技术说明；(b) `api/shared.ts:10`「后端 JSON 未按 OpenAPI 逐字段建模」——有效契约说明；(c) `theme/tokens.ts:2`「WXSS 不接受 var()」——有效约束说明（含 `xxx` 子串误配）；(d) `utils/guest.ts:10` `'xxxxxxxx-xxxx-...'`——**UUID 模板字面量误配**；(e) `web/api/dashboard.ts:24,50` `DashboardTodoItem`——**类型名含 "Todo"**（业务语义「待办」）误配；(f) `web/utils/icon.ts:5`「`<img :src="icon.xxx">`」——文档示例误配。 | `Get-ChildItem client/src,web/src,server/src -Recurse -Include *.vue,*.ts,*.java,*.xml | Select-String -Pattern 'TODO|FIXME|XXX|HACK'` → 8 命中，**真待办 0**。 | 无技术债待办标记残留，代码库无「注释里的 TODO 已失效」问题。**但反向风险**：零 TODO 也意味着已知技术债未在代码内就近标注，依赖外部文档（`docs/loop/audit/*`）承载。 | 无需整改；作为底数登记。 | S |
| QA-09 | 冗余量化 | `client/src/pages/me/my-reviews/index.vue:66-77,338`；`pages/me/notifications/index.vue:42-54,260`；`pages/home/HomeContent.vue:3-16,145`；`pages/find/index.vue:88-99,342` | **「加载失败 · 点击重试」失败态块被完整复制 4 份**（MP-012 同族视觉：`bg-soft` 凹陷面 + 大圆角 + 次级文字 + `aria-label="加载失败，点击重试"` + `hover-class="pressed"` + `@tap` 上抛 retry）。每份含独立模板块 + 独立样式块 + 独立 `loadFailed/searchFailed` 状态与重试分支。 | `Get-ChildItem client/src -Recurse -Include *.vue,*.ts | Select-String -Pattern '加载失败' -SimpleMatch` → **25 命中**；其中 4 个页面各自实现完整重试块（`my-reviews`/`notifications`/`HomeContent`/`find`），注释亦自认「与 find/feed 重试块**同族视觉**」。`加载失败 · 点击重试` 字面量 9 命中。 | 违反 spec §2「pages 中多包高频复用者上提至 components」的组件组织原则：4 处同族 UI 未抽为公共组件，任一样式/无障碍调整需改 4 处，漂移风险高。 | 上提为公共组件（如 `components/RetryBlock.vue`，接收 `title/hint/aria-label` + `retry` 事件），4 处替换；走 §2 双向治理流程并保持 type-check + `mp-weixin` 构建全绿。 | M |
| QA-10 | 冗余量化 | `web/src/components/UserActivityModal.vue:102`；`web/src/views/admin/ApplyReviewView.vue:218,261`；`web/src/views/canteen/DishDetailView.vue:472,509`；`web/src/components/InlineSelectPanel.vue:168` | Web 端以 **Unicode 字符 `★`/`✓` 作为图标/评分呈现**，共 4 处 `'★'.repeat(...)` 写法（`UserActivityModal` / `ApplyReviewView` ×2 / `DishDetailView` ×2）+ 1 处 `✓` 选中勾。每处均为内联 `repeat()` + 独立 `.stars`/`.star-off`/`.isp-check` 样式。 | `Get-ChildItem web/src -Recurse -Include *.vue,*.ts | Select-String -Pattern '[\u2600-\u27BF]'` → 6 命中（`★`×5 组合 + `✓`×1）。 | spec §4.9「禁止 emoji / Unicode 字符当图标」**适用范围明示限小程序端 `client/`**，故本条**不构成 web 红线违规**；但作为冗余量化事实登记：同一「星级呈现」在 3 个视图重复实现，unify 空间明确。 | 抽为 Web 侧 `StarRating` 展示组件（或复用 `StatusTag` 族），消除 4 处 `'★'.repeat` 重复；`✓` 改用 Element Plus 内置图标。 | S |
| QA-11 | 规模指标 | `client/src`、`web/src`、`server/src/main` | **代码规模快照**（详见文末「规模指标总表」）。三端总量：`client` 11081 行 / `server`（java）10466 行 / `web` 8156 行；后端 18 个 Controller / **57 个映射注解端点**（16 个类级 `@RequestMapping`）；小程序 9 注册页 / 25 个 `.vue`（9 页 + 16 包内私有件）/ 11 个公共组件；Web 14 视图 / 20 组件 / 18 个 api 模块。 | 见文末总表逐项命令。 | 用于判断「功能是否在泛滥」：**注册页数稳定为 9**（spec §2.1 与 `pages.json` 一致），端点总数 57 且与 `api-design.md` 主要条款可对上——**规模未见异常膨胀**。 | 作为底数登记；后续迭代以此为基线比对。 | S |
| QA-12 | 规模指标 | `git log --numstat`（最近 10 次提交） | **增删行趋势**：`e55fe46 +247/-29`、`6b3e884 +187/-8`、`32c2bae +908/-23`、`ca3205d +1103/-2009`、`c7f7574 +200/-74`、`0f618aa +58/-3`、`77759f1 +26/-17`、`3c128eb +1/-0`、`a63a942 +25/-21`、`266a5d8 +90/-9`。10 次提交**净变化 = +518 行**（累计新增 2845 / 删除 2197）。 | `git --no-pager log --numstat --pretty=format:"@@%h" -10` 聚合。 | 趋势判读：单一提交峰值出现在 `ca3205d`（§7.15 录入动线改造，-2009 行为成规模删除，为**收敛型**改动）；`32c2bae`（§7.16 恢复菜品详情页 +657 行新页）为最大新增。整体**新增与删除同量级**（2845 vs 2197），呈现「边加边收」而非单向膨胀。 | 作为底数登记。 | S |
| QA-13 | 规模指标 | `server/src/main/resources/db/schema.sql`、`seed_data.sql` | **数据库基线 12 张表**（`canteen/category/dish/email_verification_code/notification/operation_log/review/review_useful/stall/user/user_feedback/view_log`），与 spec §0.5「数据库基线 14 → 12 张表」口径**一致**。`schema.sql` 含 20 条 `CREATE TABLE IF NOT EXISTS` 语句但去重后 12 张（含幂等重建段）。 | `Select-String -Path schema.sql -Pattern 'CREATE TABLE IF NOT EXISTS \`([a-z_]+)\`'` → 去重后 **12** 张，唯一值清单见上。 | 表数量符合定型口径，无「幽灵表」残留（`activity`/`broadcast`/`apply_action` 已确证删除）。 | 作为底数登记。 | S |
| QA-14 | 冗余量化 | `client/src` 全端 | **`@click` / 裸 `scale(` / 具名 slot / `../` 跨层逃逸 四项红线 grep 均为 0**，属**通过项**登记（非缺陷）：① `@click` → **0**；② 裸 `scale(` → **0**（`client/` 全量，含 `.vue`/`.ts`/`.scss`）；③ `WaterfallList` 具名 slot → **0**（该组件已内联合并入 `HomeContent.vue:18-30`，直接渲染 `DishCard`）；④ `template #x` → **0**；⑤ `@/components` 自引用 → **0**；⑥ `from '../` 跨层逃逸 → **0**；⑦ `as any` → 9 处**全部**为 `globalThis as any`（`wx` 平台句柄），且均含「平台例外」注释（`http.ts:140,278`、`upload.ts:41`、`ImagePicker.vue:110,125,146,165`、`useDishPage.ts:140`、`App.vue:9`）。 | 逐项 grep 计数见左。 | 小程序端 UI 结构性红线**全部达标**，无阻塞级违规。 | 无需整改；作为通过证据登记。 | S |
| QA-15 | 冗余量化 | `client/src/theme/tokens.ts:4-41` 与 `client/src/App.vue:31-134` | **主色/token 三处并存**（`tokens.ts` 常量、`App.vue page{}` CSS 变量、`uni.scss` 快照）——`tokens.ts:4 primary='#C45549'` 与 `App.vue:31 --color-primary:#C45549` 同值（一致），`uni.scss:13` `#7A241A`（冲突，见 QA-01）。三处中**两处同值、一处冲突**，属同一事实的不同切面，此处量化「token 定义点数量 = 3」以支撑 QA-01 的整改范围判断。 | `Select-String -Path client/src/theme/tokens.ts -Pattern '#[0-9a-fA-F]'` → 34 处；`App.vue` → 26 处；`uni.scss` → 41 处。 | 主色改动的同步点至少 3 个（若含 `pages.json` 则 4 个），漏改面大。 | 收敛为「`tokens.ts` 常量 → 构建期生成 `App.vue` 变量块」或至少删除 `uni.scss` 冗余定义（与 QA-01 合并整改）。 | S |

---

## 规模指标总表

### 1. 三端代码规模

| 端 | 范围 | 行数 | 文件数 | 备注 |
|---|---|---|---|---|
| client | `client/src/**`（*.vue,*.ts,*.scss,*.json） | **11081** | 81 | pages 6214 / components 2176 / api 929 / stores 653 / utils 362 / types 189 / theme 73 / composables 38 |
| server | `server/src/main/java/**`（*.java） | **10466** | 157 | 另有 `resources/mapper/*.xml` 409 行、`db/schema.sql`+`seed_data.sql` |
| web | `web/src/**`（*.vue,*.ts,*.css） | **8156** | — | components 2957 / views 2892 / api 900 / styles 629 / stores 254 / utils 169 / types 117 / constants 49 / router 33 |

命令：`(Get-ChildItem <dir> -Recurse -Include <exts> -File | ForEach-Object { (Get-Content $_.FullName -Encoding UTF8 | Measure-Object -Line).Lines } | Measure-Object -Sum).Sum`

### 2. 页面 / 组件 / 接口计数

| 指标 | 数字 | 命令/佐证 |
|---|---|---|
| 后端 Controller 类 | **18** | `Get-ChildItem server/src/main/java -Recurse -Filter *Controller.java` |
| 后端映射端点（注解级） | **57** | `Select-String -Pattern '@(Get|Post|Put|Delete|Patch)Mapping'` |
| 后端类级 `@RequestMapping` | **16** | `Select-String -Pattern '@RequestMapping'` |
| 后端 Service / ServiceImpl / Mapper / VO / Req | 17 / 16 / 12 / 21 / 12 | 同上按后缀计数 |
| 小程序**注册页数** | **9**（主包 3 + 分包 2 root / 6 页） | `client/src/pages.json`，与 spec §2.1 一致 |
| 小程序 `.vue` 总数 | **25**（9 注册页 + 16 包内私有/公共件） | `Get-ChildItem client/src/pages -Recurse -Filter *.vue` |
| 小程序公共组件（`components/`） | **11** | `AppButton/AppHeader/AuthSheet/BaseSheet/CardSection/FilterBar/IconSvg/ImagePicker/SectionTitle/TabBar/TagLabel` |
| 小程序 `pages/<包>/index.vue` 页面渲染主文件 | **9** | 与注册页一一对应 |
| Web 视图 | **14** | `web/src/views/**/*.vue` |
| Web 组件 | **20** | `web/src/components/**`（含 layout 子目录 5 个） |
| Web api 模块 | **18** | `web/src/api/*.ts` |
| 小程序 api 模块 | **11** | `client/src/api/*.ts` |
| 数据库表 | **12** | `schema.sql` 去重 `CREATE TABLE` |

### 3. 最近 10 次提交增删趋势

| 提交 | 新增 | 删除 | 净变化 | 主题 |
|---|---|---|---|---|
| e55fe46 | 247 | 29 | +218 | §7.18 首页辣度筛选 + 通知全部已读 |
| 6b3e884 | 187 | 8 | +179 | §7.17 后台上传自动压缩 + geo-prompt 下沉 |
| 32c2bae | 908 | 23 | +885 | §7.16 恢复菜品详情页 + 反馈回复必填 |
| ca3205d | 1103 | 2009 | **-906** | §7.15 食堂/档口改为随菜品维护（收敛型） |
| c7f7574 | 200 | 74 | +126 | §7.14 浏览量去重 + 评价按有用数置顶 |
| 0f618aa | 58 | 3 | +55 | fix(image) 缩略图 404 白图 |
| 77759f1 | 26 | 17 | +9 | docs 迁移执行完成 |
| 3c128eb | 1 | 0 | +1 | docs 分支策略 |
| a63a942 | 25 | 21 | +4 | docs ITER-003 闭环 |
| 266a5d8 | 90 | 9 | +81 | §7.13 D 批次收尾 |
| **合计** | **2845** | **2197** | **+648** | 新增/删除同量级，呈「边加边收」 |

命令：`git --no-pager log --numstat --pretty=format:"@@%h" -10`

### 4. 红线 grep 通过证据（miniprogram client）

| 检查项 | 期望 | 实测 | 结论 |
|---|---|---|---|
| `@click` 事件绑定 | 0 | **0** | ✅ |
| 裸 `scale(` | 0 | **0** | ✅ |
| 裸 hex（组件层） | 0（例外：`App.vue`/`tokens.ts`） | 组件层 **0**；hex 仅存 `tokens.ts`(34) + `App.vue`(26) + `pages.json`(2，见 QA-02) | ⚠️ QA-02 |
| 具名 slot 分发 / `template #x` | 0 | **0** | ✅ |
| `@/components` 自引用 | 0 | **0** | ✅ |
| `from '../` 跨层逃逸 | 0 | **0** | ✅ |
| `as any`（限平台例外 + 注释） | 仅平台句柄 | **9 处全部为 `globalThis as any` + 「平台例外」注释** | ✅ |
| IconSvg 回退目标 | 中性 `empty` | `IconSvg.vue:141` → `ICONS[props.name] \|\| ICONS.empty` | ✅ |
| 破图兜底图标 | 中性 `empty` | `ImageFallback.vue:5` → `name="empty"` | ✅ |
| composable 生命周期位置 | 函数体内 | `useDishPage.ts` / `useFeedback.ts` 全部 2 空格缩进（函数体内） | ✅ |

### 5. 构建门禁实测

| 端 | 命令 | 结果 |
|---|---|---|
| server | `mvn -q clean compile -DskipTests` | **EXIT=0**（无输出即成功） |
| client | `npm run type-check`（`vue-tsc --noEmit`） | **EXIT=0** |
| client | `npm run build:mp-weixin` | **Build complete, EXIT=0**；`dist/build/mp-weixin/app.js` 存在 |
| web | `npm run build`（含 `vue-tsc --build`） | **built in 3.62s, EXIT=0**（仅第三方 `@vueuse/core` pure-annotation 警告，非本项目代码） |

---

## 与既有审计的去重声明

- QA-01（uni.scss 双真源）：spec §4.2 已自认「待清理」，**未被 TL/BE/CL/UX/WEB 任一份登记**，为新增事实。
- QA-02（pages.json 裸 hex）：`api-design.md §9` 仅登记「find confirmColor」且该处已整改为 `MODAL_CONFIRM_DANGER_COLOR`；`pages.json` 2 处**未被任何报告登记**。
- QA-03（api/category.ts 孤儿模块）：与 CL-02 同一事实，此处**补充「孤儿模块」定性**（CL-02 表述为「接口未接通」）。
- QA-04/05/06：孤儿资产与幽灵注释，**均未被既有报告登记**。
- QA-07（StatsController `@RestController` 零映射）：与 TL-07（架构债：7-Mapper 注入）**互为补充的独立事实**（TL-07 未提「零映射 + 文档幽灵条目」）。
- QA-08/09/10/11/12/13/14/15：TODO 全量、冗余量化、规模指标、红线通过证据，**均非既有报告范围**。
- 既有已登记问题（TL-01~15、BE-01~15、CL-01~15、UX-01~12、WEB-01~15）**本文不重复**。
