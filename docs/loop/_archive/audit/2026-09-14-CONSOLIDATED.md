# 审计汇总与业务原则草案（CONSOLIDATED）

> 主导：产品经理（本 loop 唯一主导者）｜2026-09-14
> 输入：`docs/loop/audit/2026-09-14-{tech-lead,backend,client,web,uiux,qa}.md` 六份审计报告（逐条读证）＋ `docs/project_spec.md`（红线 / §0.5 明确不做 / §7 全部决议）＋ `docs/loop/STATUS.md` ＋ `docs/loop/QUESTIONS.md`
> **本轮定位：只做「收敛与闭环」的汇总与立原则，不扩张功能、不写业务代码、不改 `project_spec.md`/`tasks/`。**
> **第一红线遵守声明**：凡已被 §7 拍板否决 / 明确不做者（收藏、社区/动态、活动+公告、下单/支付、售罄、随机推荐、榜单、分享、搜索历史、评价排序切换、订阅消息、Excel 导入、一键转菜品、只读管理员口令、多校区、评价 tags、档口营业时间、学生端菜品写接口）**一律不出现在本文件**，亦不转化为「建议以后再做」。本文件不产生任何新功能清单。

---

## 一、问题总表（跨角色去重合并后）

> 合并规则：**同一根因只留一条**，关联编号并列标注。共 **去重后 44 条**（六份报告原始条目合计约 84 条，合并压掉约 40 条同源重复）。
> 严重度：`P0 上线阻断` / `P1 决议未落地（文档对、代码错）` / `P2 规则空缺（需用户定原则）` / `P3 冗余与清理`。

### P0 上线阻断（7 条）

| 编号 | 严重度 | 关联 | 一句话现象 | 影响 | 处置建议 | 需用户决策? | 成本 |
|---|---|---|---|---|---|---|---|
| P0-01 | P0 | BE-01（独有） | 管理端 `DishAdminReq` 声明 `spiceLevel`/`portion`，但 `DishServiceImpl.applyReq:245-263` **从不写入**（全仓 `setSpiceLevel/setPortion` = 0 处），字段恒为默认值 | §7.18「首页辣度筛选」已上线但**按辣度筛选恒返回空集**——已拍板功能表面可用、实际全废；属假功能 | `applyReq` 补 `setSpiceLevel/setPortion` 并加值域校验（0-3 / 0-2） | 否 | S |
| P0-02 | P0 | TL-04（独有） | `seed_data.sql:172-192` 仍 `UPDATE dish SET ... serve_period=...`，而该列已由 `schema.sql:603-621` 幂等迁移 DROP | 标准流程「先 schema 后 seed」下**新环境建库必因 Unknown column 报错**，破坏建库脚本自包含性（红线：脚本可重跑） | 删除 172-192 段及 `WHERE` 中的 `serve_period`，保留 `spice_level/portion/region` 赋值；本地跑一遍 seed 验证零报错 | 否 | S |
| P0-03 | P0 | WEB-01（独有） | 后端 `reply` 已 `@NotBlank` 必填，但 Web `FeedbackView` 标注「选填」、文案「留空则通用回执」、`submitHandle` 无校验直接提交 | **管理员每次留空必吃 400**，与 §7.16 第 2 条「回复必填」直接矛盾；属上线必经路径硬缺陷 | 标签改「（必填）」、提交前 NotBlank 校验、删「选填/通用回执」文案 | 否 | S |
| P0-04 | P0 | QA-01 / QA-15 | `client/uni.scss` 存在**第二套 design token 真源**（`--color-primary:#7A241A`）与事实源 `tokens.ts`/`App.vue`（`#C45549`）冲突；uni-app 编译期自动注入每个组件 style | 违反 §4.2「色值全站唯一事实源」；后续组件一旦引用 uni.scss 独有键将**静默取到陈旧值**，改主色存在漏改风险 | 删除 `client/uni.scss`（或清空为注释）；13 个独有键确认无消费后移除 | 否 | S |
| P0-05 | P0 | CL-03 / UX-03 | `FilterBar.vue:53-60`「筛选」胶囊有 `role="button"`+`aria-label="详细筛选"`，但**无 `@tap`、无 emit**，外观与相邻可点胶囊同款 | **假控件交互欺骗**：承诺可点却无反应，用户反复点；违反「点按必反馈」 | 二选一：移除该胶囊；或明确承载内容后挂事件。建议移除 | 是（见 Q-105） | S |
| P0-06 | P0 | TL-01 / UX-01 / UX-02 | 端上**唯一评价卡组件** `DishReviewSection.vue:16` 硬编码 `hide-useful` 隐藏「有用」按钮，且 `useDishPage.ts` 6 处显式传 `sort:'latest'` 覆写后端默认 | §7.14 第 2 条「按有用数置顶」与 §7.18 第 3 条「维持有用数置顶」在端上**完全未生效**；`POST /reviews/{id}/useful` 全链路无入口、排序永远时间序（后端接口/计数/置顶 SQL 全为死资产） | 二选一：(a) 恢复「有用」按钮 + 去 `sort:'latest'` 覆写使决议生效（推荐）；(b) 撤回 §7.14 第 2 条/§7.18 第 3 条并下线接口 | 是（见 Q-104） | M |
| P0-07 | P0 | TL-03 / TL-02 / BE-15 | spec §0.1/§0.4/§3/§5.y.5 仍写「方案 C：`/auth/admin/login`+BCrypt+JWT / `ADMIN`+`SUPER_ADMIN` 双层 + `meta.role` 守卫双闸门」，实况已整体改为 `X-Admin-Token` 单口令（无登录页/无角色），**同文件 §7.10 第 2 条自相矛盾**；`api-design.md:174` §5 前言同余毒 | 联调方按文档实现直接 403；后续 agent 可能据错的那套去补一个已被口令替代的机制 | 以 §7.10 第 2 条为准，**单向**把 §0.1/§0.4/§3/§5.y.5 与 `api-design.md` §5 收敛为「`X-Admin-Token` 环境变量口令、未配置 fail-closed 403、无角色体系」；清理三处残留注释/Swagger 文案 | 否（技术负责人执行） | M |

### P1 决议未落地（文档对、代码错）（6 条）

| 编号 | 严重度 | 关联 | 一句话现象 | 影响 | 处置建议 | 需用户决策? | 成本 |
|---|---|---|---|---|---|---|---|
| P1-01 | P1 | WEB-03 / WEB-14 / BE-02 / TL-09 | `/admin/audit/**` 三条接口 + `api/audit.ts` 三函数 + `AuditVO` 三层齐备但**前端零入口**；`dish_audit` 通知唯一触发点是「学生菜品提交」，而学生写接口已下线（管理员录入即 approved） | 后台**看不到也无法处置**任何 pending 实体；`dish_audit` 为不可达分支（仅服务历史存量）。一旦出现存量 pending 将**无法处置** | 二选一：① 补「实体审核」分类卡（消费既有接口）；② 确认永不启用则物理删除 `api/audit.ts`+`auditToLegacy`+`AuditVO` 并登记。`dish_audit` 至少显式登记为存量兼容 | 是（见 Q-107） | M |
| P1-02 | P1 | WEB-02（独有） | `DishFormDialog` 渲染「搜索别名」输入框但 `submit()` payload **不含 `alias`**；`DishDetailView.editForm` 无 alias 字段 | §0.0 定型增补「`dish.alias` 别名搜索本期落地（管理员配置）」在 Web 侧是**死入口**，管理员以为填了实际不落库 | `DishFormDialog.submit()` payload 补 `alias`；`DishDetailView` 声明/回填/展示/取消各补 `alias` | 否 | S |
| P1-03 | P1 | WEB-05（独有） | 后端 `GET /admin/dashboard` 返回 `pendingFeedbacks`（待办明细 5 条）与 `recentLogs`（近期操作 10 条），前端声明类型却**从未渲染** | §0.4.1 明确「成功渲染三块」，实况只渲染待办计数 + 规模指标；管理员看不到「是哪 3 条」、无「近期操作」 | `DashboardView` 补「待办明细」列表（点击直达反馈）与「近期操作」区块 | 否 | M |
| P1-04 | P1 | WEB-04（独有） | 反馈详情「关联菜品」跳转携带 `?tab=dish&q=<菜名>`，但 `DishManageView` **未读 `route.query`** | 「反馈→找到菜品→修正」运营主路径**断在最后一跳**，管理员须手敲菜名 | `DishManageView` 读 `route.query.q` 作 `searchQuery` 初值并 watch 同步 | 否 | S |
| P1-05 | P1 | BE-13（独有） | 价格**无值域校验**：`price/originalPrice/promoPrice` 允许 0/负数；`promoPrice > price`、`originalPrice < promoPrice` 等矛盾组合无约束 | 可落库 `price=-100` 或「促销价高于原价」，前端出现负数金额/折扣倒挂；§3 分单位红线只约束量纲未约束值域 | Service 校验：`price>0`、`promoPrice>0`、`originalPrice>0`（非空）、`promoPrice<=originalPrice`（非空），非法 400 | 否 | S |
| P1-06 | P1 | CL-04 / UX-07（关联 UX-02 同源） | §7.5/§7.7 要求「已认证但无 openid」返回 403 时端上给「请先微信登录」引导，实况 `http.ts` 仅 toast 不弹引导；评价区无失败态（静默吞成「无评价」）、零评价无空态引导 | 该分支用户**被彻底卡死**（无法完成本可完成的写评价）；评价加载失败被误认为「确实没人评」 | 端上对该 403 做识别给可执行引导（不新增页面）；评价区补 MP-012 失败态与零评价引导 | 否 | S |

### P2 规则空缺（需用户定原则）（11 条）

| 编号 | 严重度 | 关联 | 一句话现象 | 影响 | 处置建议 | 需用户决策? | 成本 |
|---|---|---|---|---|---|---|---|
| P2-01 | P2 | BE-03 / BE-05 / BE-04 / BE-07 | 写入/筛选参数**均无白名单**：`FeedbackReq.type` 仅 `@NotBlank`（可写 `foobar`）；`dish.tags` 原样落库（schema 声明「仅允许登记值」）；admin 的 `status/type/secState` 非法值直接进 SQL 恒空；`review.sort` 非法值静默落 `useful` 分支 | 脏数据污染筛选维度；拼错筛选项返回空列表**掩盖真实积压**；排序「以为切了却没切」 | 统一在 Service 层做枚举/值域白名单校验，非法 400；常量类为单一真源 | 否（原则定后即可实现） | S |
| P2-02 | P2 | BE-06 / TL-18(§7.13#4) | `DishHeatWeights` 常量**全仓零引用**，热度公式只活在 `DishMapper.xml:heatScoreExpr`；§7.13 第 4 条要求二者「保持等价」但无任何编译期/测试期保障 | Java 侧常量改错**不报错**，纯靠人工同步；易误以为改了它就会生效 | 二选一：① 删除常量、口径唯一收敛到 SQL；② 保留并注释「当前无运行时引用，仅作口径登记」 | 是（见 Q-109） | S |
| P2-03 | P2 | BE-09（独有） | 评分聚合为 AFTER_COMMIT + `@Async`，失败仅 `log.error("[ALERT]")` 后**丢弃**（无重试/补偿/对账）；且统计口径 `is_hidden=0`，**不含 `sec_state`** | 聚合丢失后 `avg_rating/rating_count` **永久漂移**（首页排序/热搜都按它算）且无人发现；`sec_state='review'/'rejected'` 评价是否计入评分口径未定 | ① 提供手动/定时全量对账重算入口；② 明确登记评分统计口径是否叠加 `sec_state` | 是（见 Q-110） | M |
| P2-04 | P2 | BE-08 / BE-10（独有） | 级联口径三处互不一致：`deleteDish` 有事务但不含 `view_log`（残留孤儿行）；`migrateOwnership` 删冲突 review 前未清 `review_useful`（孤儿行）；`/admin/dishes` 新增/编辑与食堂/档口 CRUD **未标 `@Transactional`** | 删除/合并后留下指向不存在对象的孤儿行，「猜你喜欢」读取链路可能拿到无效 ID | 补 `@Transactional`；`deleteDish` 事务内追加清理 `view_log`；`migrateOwnership` 按 `deleteReview:315-316` 同口径先清 `review_useful` | 否 | M |
| P2-05 | P2 | TL-15（独有，争议） | §7.7 第 1 条明写 UGC 准入不满足时统一返回 **403**，代码实际拆成 `4031`（未认证，前端弹引导）+ `403`（已认证无 openid，仅 toast） | 决议与实现**同时成立但口径不同**，客户端无法从 403 区分「无 openid」与「越权」 | 保留代码的 4031/403 分流（更合理），据此修订 §7.7 第 1 条表述（须用户确认修改既有拍板文本） | 是（见 Q-111） | S |
| P2-06 | P2 | BE-12 / BE-14 | `portion`（分量）端上零消费且后端从不写入（属「读不到又没人用」）；`PUT /auth/password` 无角色闸门、对学生账号语义不成立（`user.password` 恒 NULL），而 §5.y.1 已声明该端点**废弃移除** | 悬置字段易被误认为在用；路径挂在学生侧、语义属管理端的接口归属不清 | `portion` 走 §7.9 同款「字段下线」评估后移除；`/auth/password` 删除（对齐 §5.y.1 废除口径，推荐）或加 `hasRole('ADMIN')` 回写 spec | 是（见 Q-108） | S |
| P2-07 | P2 | TL-05 / QA-02 | `schema.sql` 内 `add_stall_phase1_fields` 先**新增** `stall.business_hours`、文件末尾再 DROP（自相矛盾）；`pages.json:88-89` 两处裸 hex 与 `tokens.ts` 同值但未引用常量、未登记 | 脚本徒增 DDL 与阅读成本、易误认为该列仍在册；改色时 `pages.json` 不会同步 | 从 add 段移除 `business_hours`、修正 `schema.sql:244` 注释；`pages.json` 两处在 `tokens.ts` 注释登记消费点 | 否 | S |
| P2-08 | P2 | TL-06 / TL-10 / QA-07 | `StatsController` 每次请求跑全表菜品聚合 + 最多十余条趋势 SQL，而 §0.4.1 已明示前端**不消费**这些图表字段；`viewTrend` 实为「每日新增菜品数」却命名浏览量趋势；`api-design.md:180` 记载 `GET /admin/stats/**` 但**全仓零命中**（幽灵接口） | 后台首屏承担数十条无用查询 + 一次全表扫描；文档存在幽灵端点 | 图表字段改懒计算或从 `overview()` 移除、修正 `viewTrend` 命名；删除 `api-design.md:180` 幽灵行 | 是（见 Q-106） | M |
| P2-09 | P2 | TL-16 类（CL-01 / CL-10 / UX-04 / TL-19 缺失原则 2） | 排序状态**存在但无 UI 入口**：`homeSortBy`/`findSortBy` 恒 `'latest'`、`setHomeSort` 零调用、`priceAsc/hot/distance` 分支全不可达；后端默认 `useful`、端上显式 `latest`，**「默认排序唯一权威方」归属未定** | 「当前排序」不可见不可改、状态死；缺「默认排序归属」原则将反复扯皮 | 删除死分支收敛为单一默认（热度优先，与 §7.17 第 2 条一致） | 是（见 Q-105，与 P0-05 合并） | S |
| P2-10 | P2 | BE-16 类（TL-09 / TL-07 / QA-06 / WEB-11） | 一系列**悬空承诺/幽灵注释**：`DishAdminController:61` Swagger 自认「后续需要补权限边界」（单口令下永不成立）；`CanteenServiceImpl:143` 引用不可追溯的 `task todo#3`；`@Tag` 声明「数据统计（数据看板）」端点并不存在 | 悬空承诺持续误导后人去补已被替代的机制；幽灵注释使后人以为存在未完成任务 | 清理三处悬空文案；`StatsController` 更名 `DashboardService` 并移入 service 层 | 否 | S |
| P2-11 | P2 | CL-01 / CL-02 / CL-05 / CL-09 / CL-11 / CL-12 / CL-13 / CL-14 / CL-15 | 一批「端上零消费/死状态/幽灵能力」：`getCategories` 零调用（品类滚轮死链）；`reviewsDirty` 无处置 true（死机制）；`withLocalDistance(sort=true)` 不可达；`routeMap` 导出面过宽、`syncRoute` 不可达；`updateCanteen/deleteStall` 等零消费；`uploadImage` 命名易误用致 UGC 绕安检；反馈页食堂树 `any` 逃逸；`mine.showBack` 恒 false | 死代码误导后续开发、增大误用面；`uploadImage` 误用**可致 UGC 绕过内容安检**（红线级） | 逐条删除死分支；`uploadImage` 更名 `uploadAvatarImage` 并在注释标明禁止用于 UGC；食堂树补最小 DTO 消除 `any` | 是（品类去留见 Q-103，其余否） | S |

### P3 冗余与清理（20 条）

| 编号 | 严重度 | 关联 | 一句话现象 | 影响 | 处置建议 | 需用户决策? | 成本 |
|---|---|---|---|---|---|---|---|
| P3-01 | P3 | CL-06（独有） | §4.9 红线要求分区标题一律 `<SectionTitle>`；`DishSummaryCard` 手写 `.summary-head`、`DishReviewSection` 手写 `.review-card-head` | 违反 §4.9 分区标题红线（BLOCKER 级审计项）；同页出现三套标题实现，字重间距漂移 | 两处改走 `SectionTitle`（支持 `extra` 槽承载评价数），删手写样式 | 否 | S |
| P3-02 | P3 | BE-11（独有） | `ReviewServiceImpl.encodeImages` 与 `FeedbackServiceImpl.encodeImages` **逐行近乎完全重复**（≤3 张 + 域名白名单 + 序列化） | §5.a 配图契约存在两份实现，改上限/白名单易只改一处导致口径分叉 | 抽公共组件（如 `common/utils/UgcImageValidator`），错误文案参数化 | 否 | S |
| P3-03 | P3 | QA-09（独有） | 「加载失败·点击重试」失败态块在 4 个页面**各自完整复制**（my-reviews/notifications/HomeContent/find） | 违反 §2「高频复用者上提 components」；样式/无障碍调整需改 4 处 | 上提 `components/RetryBlock.vue`，4 处替换 | 否 | M |
| P3-04 | P3 | QA-03 / CL-02 | `api/category.ts` 整模块**零引用**（`getCategories` 无调用方），后端 `GET /categories` 端上零消费 | 死接口 + 死模块，增大维护面与误用面 | 确认无规划后整体删除（与 Q-103 合并） | 是（见 Q-103） | S |
| P3-05 | P3 | QA-04（独有） | `client/src/types/related-item.ts` 整文件零引用（`RelatedItem` 无 import 方） | 孤儿类型（疑似随关联链路下线残留） | 删除该文件 | 否 | S |
| P3-06 | P3 | QA-05（独有） | `IconSvg` 3 个图标键零消费（`heart-filled`/`thumb-filled`/`send-simple`）；`heart` 键亦零消费 | 死图标资产；「喜欢/心形」语义资产与「无收藏」口径的残留关联 | 删 3 个零消费键；`heart/heart-filled` 存废与 §5.x「喜欢/收藏单一概念」对齐后决定 | 否 | S |
| P3-07 | P3 | WEB-11（独有） | `pageStore.breadcrumbs` 5 处写入、**0 处读取**（`PageContainer` 只接 prop） | 死 store + 5 处死写入；形成两套并存「面包屑」语义 | 删 `pageStore` 与 5 处 `setPage`（后台顶部导航已表达层级） | 否 | S |
| P3-08 | P3 | WEB-12（独有） | `adminStore` 的 `activeCanteens/activeStalls/activeDishes/stats/todayOrders` **零消费**（含恒为 0 的「今日订单」，与无下单定位相悖） | 死代码与本项目定位相悖，误导 | 删除这些成员（含 `todayOrders`）；再评估 store `activeList` 三链是否一并下沉 | 否 | S |
| P3-09 | P3 | WEB-08（独有） | 操作日志前端 `actionOptions` 只登记 7 项，后端常量 **12 个**（缺安检复核/品类 5 项、多出恒空的 `audit_*`）；列表单元格落**裸英文枚举** | 按「安检复核/品类」筛选不可行；日志出现裸英文；审核筛选项恒空误导排查 | 对齐后端 12 个常量（`audit_*` 去留随 P1-01）；单元格走文案映射 | 否 | S |
| P3-10 | P3 | TL-08 / BE-03 | 反馈类型后端仍开放 **6 种**，小程序端只产出 **3 种**（`report` 走详情页、`bug/other` 无生产者）；spec §2.1.4 亦记「实况 3 类」 | 零生产者开放枚举；管理端筛选下拉出现永远为空的选项 | 收敛枚举：移除 `TYPE_BUG/TYPE_OTHER`（或登记为「历史遗留、禁新增」），同步三处 `@Schema` 与筛选项 | 否 | S |
| P3-11 | P3 | TL-12（独有） | `PageResult.of(records,total)` 两参重载已 `@Deprecated` 且**全仓零调用**；`api-design.md:43` 称「部分接口形态为 `{list,total}`…如 `/my/reviews`」已**过期**（现走 4 参版） | 文档描述不存在的分页形态，误导前端继续写兼容分支；死重载增噪 | 删两参重载；更新 `api-design.md:43` 注记；按 §7.11 第 2 条排期收敛 `records||list` 兜底 | 否 | S |
| P3-12 | P3 | BE-12 部分 | `UserVO` 与 `UserInfoVO` 字段几乎完全相同（仅 `createdAt` 差异），各自手工 `toVO` | 重复 VO 增加维护面 | 评估合并或在注释登记差异与用途 | 否 | S |
| P3-13 | P3 | CL-07（独有） | 标签中英映射存在「API 层翻译」与「页面直显」两套（`toDish` 已映射，`DishCard`/`DishInfoCard` 又直取） | 同一能力两处实现，语义真源不唯一 | 统一 API 层映射，清理页面层重复；核对 `TagLabel.tag-new` 死分支（§7.9 已删） | 否 | S |
| P3-14 | P3 | CL-08（独有） | 投稿 `add` 价格校验 `>0 && <=9999` 但**未校验小数位**（可填 `12.999`），提示文案却要求「像 12.5」 | 三位小数在管理员录入时被截断，投稿精度失真 | 统一「最多两位小数」校验，提示文案同步 | 否 | S |
| P3-15 | P3 | WEB-07（独有） | Web 路由**根级无 `catch-all`**，非法/历史 URL → `<router-view>` 空渲染，**全白无提示** | 误点/旧书签 → 白屏无「返回工作台」引导 | 补根级兜底 `{ path:'/:pathMatch(.*)*', redirect:'/dashboard' }` | 否 | S |
| P3-16 | P3 | WEB-13（独有） | 操作日志 API 与后端均支持 `startAt/endAt`，UI **无时间选择器**，参数永不传 | 「某天谁改了什么」无法按时间收窄 | `FilterBar` 补 `el-date-picker`(range) 绑定 `startAt/endAt` | 是（见 Q-112） | S |
| P3-17 | P3 | QA-10（独有） | Web 端以 `'★'.repeat()` 呈现星级共 **4 处** + `✓` 勾 1 处，各自内联样式（§4.9 emoji 禁令**只约束 client**，故不构成红线） | 「星级呈现」在 3 个视图重复实现，unify 空间明确 | 抽 Web 侧 `StarRating` 组件；`✓` 改 Element Plus 图标 | 否 | S |
| P3-18 | P3 | TL-13（独有） | spec §7.4 称 `ReportModal` 已挂**首页**，实况唯一挂载点是**菜品详情页**（举报对象为评价） | 文档指错页面；该句还写在「举报链路已闭环」结论里，易误导验收 | 修正 §7.4 表述为「已挂**菜品详情页**评价卡的『举报评价』入口」 | 否 | S |
| P3-19 | P3 | TL-11（独有） | §7.12 第 3 条（`d.status` 不过滤）与 §7.14 第 3 条（`is_hidden` 不过滤）**表述重叠但口径不同**，行文易被误读为同一条 | 落地时易把「菜品下架仍可见」误做成「隐藏评价照旧过滤」或反之 | 在 §7.12 第 3 条补边界说明并给 `ReviewMapper.xml` 行号锚点 | 否 | S |
| P3-20 | P3 | TL-14 / WEB-06 / WEB-09 / WEB-10 / WEB-15 / QA-11~14 等 | 已登记技术债与体验缺口合集：后台菜品筛选**前端内存过滤**（需全量拉取）未登记；食堂/档口**无改名/停业入口**（打错字永久）；编辑保存**无并发提示**可静默覆盖；删除菜品**无连带影响提示**；反馈「标记处理」**无二次确认且不可撤回**；规模指标（三端 ~2.97 万行、57 端点）与红线 grep 通过证据（`@click`/裸 `scale`/具名 slot 均 0） | 均为「代码写得出、业务走不通」或数据风险；规模未见异常膨胀（注册页稳定 9、表 12 张） | 分项：登记前端过滤技术债；食堂/档口补「编辑」入口；保存加轻量 `updatedAt` 提示；删除确认加评价影响文案；反馈处理加二次确认 | 是（食堂/档口编辑、并发、删除提示、二次确认见 Q-112） | M |

---

## 二、《业务原则草案》（PR-01 ~ PR-12）

> 用途：作为 `project_spec.md` 之外的**长期复用判据**，供三端开发、评审、门禁逐条对照。每条均可判定对错；违反即视为缺陷（与 §7 冲突时以 §7 为准）。
> 归纳来源：本文件第一节全部条目；数量控制在 12 条，宁少而硬。

| 编号 | 原则（一句话祈使句） | 依据（哪几条问题暴露它） | 反面案例（现实现如何违反） | 适用范围 | 若违反的后果 |
|---|---|---|---|---|---|
| **PR-01** | **任何面向用户的筛选/展示维度，必须有配套的数据录入来源与写入链路，否则不得放出该维度。** | P0-01、P0-06、P1-02、P2-06 | 辣度筛选已上线但 `applyReq` 从不写 `spiceLevel`（筛选恒空）；`alias` 表单有输入框但 payload 不送达；`portion` 端上零消费且后端不写 | 三端全部筛选/展示维度（辣度、风味、标签、别名、评分） | 放出即等于**假功能**：用户操作无结果，信任一次即永久流失。 |
| **PR-02** | **同一业务口径必须指定唯一权威方（后端），端上只消费不覆写；文档与代码冲突时一律以「最新拍板决议」为准，不得由代码现状反推文档。** | P0-06、P2-09、P0-07、P2-05 | 后端默认 `sort=useful`，端上显式传 `latest` 覆写；spec §7.10 与 §0.1/§0.4 并存两套管理端鉴权口径；§7.7 的 403 与代码 4031/403 分歧 | 排序/分页/鉴权/错误码等一切跨端口径 | 两端各持一套真源 → 联调 403/白屏，且**修一边坏一边**。 |
| **PR-03** | **新功能准入须同时具备「入口 + 出口 + 空态 + 失败态 + 数据来源」五要素，缺一不得上线。** | P0-05、P1-01、P1-06、P2-11、P3-15 | 「筛选」胶囊有可点外观无动作；评价区无失败态/零评价无引导；Web 无根级兜底致白屏；实体审核有接口无入口 | 三端所有新页面/新组件/新入口 | 半成品上线 → 假控件、白屏、死入口，运营与用户均无法闭环。 |
| **PR-04** | **被否决项须在「明确不做清单」留痕，并以「恢复须重新拍板」为唯一翻案路径；禁止以任何形式重提或变相保留。** | 本轮全部（审计纪律基线）、P3-04、P3-06、P3-10 | 收藏/社区/活动已下线仍残留图标与措辞；`bug/other` 无生产者却保留开放枚举；死模块 `api/category.ts` 长期驻留 | 全项目需求与文档管理 | 无留痕 → 反复「节外生枝」，产品功能泛滥，重复投入已否决能力。 |
| **PR-05** | **冗余边界：凡「对外暴露但零消费」的接口/函数/字段/常量/图标/类型一律删除；仅当「有拍板依据的预留」或「历史存量兼容」方可保留，且必须显式登记保留理由。** | P3-04、P3-05、P3-06、P3-07、P3-08、P2-02、P2-11 | `getCategories`/`pageStore.breadcrumbs`/`DishHeatWeights`/3 个图标键零消费仍驻留；`api/audit.ts` 三层齐备零入口 | 三端代码资产 | 死资产累积 → 维护面膨胀、后续开发者误以为「在用」而重复排查或误接。 |
| **PR-06** | **参数非法即报错：所有面向管理端/端上的枚举、标签、排序、筛选、金额类入参必须经单一真源白名单校验，非法值返回 400，绝不静默降级。** | P2-01、P1-05、P3-10 | `type` 可写 `foobar`；`tags` 原样落库；`sort` 非法值静默落 `useful`；`status/secState` 非法值进 SQL 恒空 | 三端所有写接口与查询接口入参 | 脏数据污染维度；静默恒空**掩盖真实积压**；排序「以为切了没切」。 |
| **PR-07** | **字段生命周期成对处置：任何字段的引入与下线必须一一对应（DTO/实体/VO/Mapper/后台表单/端上映射/建表脚本），端上零消费且后端不写的字段必须显式下线或显式登记为「仅存量兼容」。** | P2-06、P2-07、P1-01、P3-10、P3-11 | `portion` 与 `spice_level` 同批引入却处置不对称；`schema.sql` 内先 add 后 drop `business_hours`；`dish_audit` 通知无生产者仍在册 | 三端数据模型与脚本 | 悬置字段扰民、脚本自相矛盾，最终以「Unknown column」在建库时爆炸（P0-02）。 |
| **PR-08** | **聚合与冗余计数必须声明「谁是真源、何时重算、失败如何补偿」；异步聚合失败不得只有一行告警日志。** | P2-03、P2-04 | 评分聚合 `@Async` 失败即丢弃、无对账；`dish.avg_rating`/`rating_count`/`useful_count` 漂移无人知 | 后端所有派生计数字段 | 首页排序/热搜按漂移值计算 → 推荐失真且**无人发现**。 |
| **PR-09** | **删除类与合并类操作必须成对定义级联范围（评价 / review_useful / view_log / notification），三处口径保持一致。** | P2-04 | `deleteDish` 不清 `view_log`；`migrateOwnership` 删 review 前不清 `review_useful`；新增/编辑无 `@Transactional` | 后端所有破坏性写操作 | 孤儿行指向不存在对象，「猜你喜欢」拿到无效 ID，`useful_count` 长期发散。 |
| **PR-10** | **凡产生公开可见内容的图片上传必须唯一走安检链路（`uploadUgcImage`）；无安检上传函数只能用于已登记的例外（如头像），且命名须自证用途。** | P2-11（CL-09）、P2-01 | `uploadImage` 命名更「通用」，新页面极易误用无安检链路而绕过 content check | 三端所有图片上传入口 | UGC 绕过内容安检 → 合规红线失守（§5.a / §0.0 合规底线）。 |
| **PR-11** | **任何具备可点外观或无障碍可点语义的控件必须绑定真实动作；占位/装饰元素不得使用可点语义交付。** | P0-05、P2-09、P2-11 | 「筛选」胶囊 `role="button"` 无 `@tap`；排序状态存在无 UI 入口 | 小程序端与 Web 端全部交互元素 | 交互欺骗；用户反复点击；无障碍承诺与实现矛盾。 |
| **PR-12** | **枚举/常量展示必须与后端常量表同源；前端不得手写子集或裸英文枚举；跨端 DTO 必须显式定型（平台句柄外禁止 `any`）。** | P3-09、P3-10、P2-11、P3-12 | 操作日志前端只登记 7/12 动作、单元格显示裸英文；反馈页食堂树用 `any`；`UserVO`/`UserInfoVO` 双份 | 三端枚举展示与跨端类型 | 筛选项恒空/漏项；字段名变更时端上静默失效且无编译期保护。 |

> **原则与 §7 的关系**：PR-01~PR-12 是 §7 已有决议的**归纳与前置判据**，不新增能力、不改变任何已拍板结论；如有冲突以 §7 为准。后续任何「新入口/新维度/新字段」都须先过 PR-01~PR-12 自检。

---

## 三、蓝图缺口（决议未落地 + spec 自相矛盾）

> 只列 **已拍板但未实现** 或 **spec 内部互相矛盾** 的条目，并给出「以哪份文档/哪条决议为准」的**裁定建议**（产品经理职责，不推给用户）。

| 编号 | 缺口类型 | 条款 | 与实现/其它条款的矛盾 | 裁定建议（以谁为准） |
|---|---|---|---|---|
| G-01 | 决议未落地 | §7.18 第 1 条「首页辣度筛选」＋§7.18 前提「后端与数据无需改动」 | `applyReq:245-263` 从不写 `spiceLevel`，字段恒默认值 0 → 筛选恒空 | **以 §7.18 为准，改代码**：`applyReq` 补写入 + 值域校验（→ P0-01） |
| G-02 | 决议未落地 | §7.14 第 2 条「评价默认按有用数置顶」＋§7.18 第 3 条「维持单一默认」 | 端上硬编码 `hide-useful` + 6 处覆写 `sort:'latest'`；后端置顶 SQL 为死资产 | **以 §7.14/§7.18 为准，改代码**：恢复按钮 + 去覆写（→ P0-06，若确认撤回须用户重新拍板） |
| G-03 | spec 自相矛盾 | §7.10 第 2 条「管理端改环境变量共享口令、无登录体系」 ↔ §0.1/§0.4/§3/§5.y.5「方案 C / BCrypt / `/auth/admin/login` / SUPER_ADMIN / `meta.role` 双闸门」 | 同文件两套管理端鉴权口径；代码已按 §7.10 实现 | **以 §7.10 第 2 条为准**（后拍板且 §7 效力最高）：单向把 §0.1/§0.4/§3/§5.y.5 与 `api-design.md` §5 收敛为「单口令」表述（→ P0-07） |
| G-04 | spec 自相矛盾 | §7.7 第 1 条「不满足双约束统一返回 403」 ↔ `ReviewServiceImpl:212-221`「4031（未认证）/ 403（无 openid）分流」＋§3 错误码表已登记 4031 | 决议与实现同时成立、口径不同 | **建议以代码的 4031/403 分流为准**（更合理、§3 已登记 4031），据此修订 §7.7 第 1 条表述——**须用户确认修改既有拍板文本**（→ Q-111） |
| G-05 | 决议未落地 | §0.0 定型增补「`dish.alias` 别名搜索本期落地（管理员配置）」 | Web 表单有 alias 输入框但 payload 不含、详情页 `editForm` 无该字段 | **以 §0.0 增补为准，改代码**：补 payload 与详情页字段（→ P1-02） |
| G-06 | 决议未落地 | §7.16 第 2 条「处理回复改为必填」 | Web 标注「选填」、无校验、留空即 400 | **以 §7.16 为准，改 web**（→ P0-03） |
| G-07 | spec 内部表述重叠 | §7.12 第 3 条（`d.status` 不过滤）↔ §7.14 第 3 条（`is_hidden` 不过滤） | 两条讨论不同字段但行文易混为同一条 | **两条各自有效、非矛盾**：属**表述口径需精确化**，以 §7.14 第 3 条为 `is_hidden` 口径、§7.12 第 3 条限于 `status`，补边界说明（→ P3-19，技术负责人执行） |
| G-08 | 决议未落地 | §7.4「举报链路已闭环…`ReportModal` 已挂首页」 | 实况唯一挂载点为**菜品详情页** | **以实况（详情页）为准、修文档**：修正 §7.4 表述（§7.4 其余结论不变）（→ P3-18） |
| G-09 | 决议未落地 | §7.13 第 4 条「`heatScoreExpr` 与 Java 侧 `DishHeatWeights` 须保持等价」 | `DishHeatWeights` 零引用，等价性无任何保障 | **以 §7.13 第 4 条为准**：删除常量（口径唯收敛到 SQL）或保留并登记「无运行时引用」（→ Q-109） |
| G-10 | 决议未落地 | §0.4.1「工作台成功渲染三块（待办/规模指标/近期操作）＋待办明细 5 条」 | 前端只渲染待办计数 + 规模指标，`pendingFeedbacks`/`recentLogs` 声明未渲染 | **以 §0.4.1 为准，改 web**：补「待办明细」与「近期操作」区块（→ P1-03） |
| G-11 | 决议未落地 | §0.4.1 边界「工作台不含图表、`DashboardVO` 图表字段前端不消费」 | 后端每次请求仍全量计算图表字段 + 全表扫描；`viewTrend` 命名与口径不符 | **以 §0.4.1 为准**：图表字段懒计算或移除（→ Q-106） |
| G-12 | 决议未落地 | §7.8 第 1 条「管理员录入即 approved」「审核中心保留给学生投稿链路」 | 学生菜品写接口已下线 → pending 无来源；`/admin/audit` 与 `dish_audit` 通知成不可达分支 | **以 §7.8 第 1 条为准**：实体审核是否保留须用户拍板（存量 pending 是否为 0）（→ Q-107） |
| G-13 | 决议未落地 | §5.y.1「`/auth/password` 废弃移除」 | 端点仍在（无角色闸门、学生语义不成立） | **以 §5.y.1 为准，删端点**（推荐），或加 `hasRole('ADMIN')` 并回写（→ Q-108） |
| G-14 | 决议未落地 | §4.2「色值为全站唯一事实源」 | `uni.scss` 第二套真源与 `tokens.ts`/`App.vue` 冲突 | **以 §4.2 为准**：删除 `uni.scss` 冗余定义（→ P0-04） |
| G-15 | spec 与代码双向冲突（已由 §7.10 单向裁定） | §0.3 L121「`/admin/**` 仅 `ADMIN`（含 SUPER_ADMIN 分层）」 | 单口令模型下 `RoleConst` 自认「无角色层级」，全仓 `@PreAuthorize("hasRole('ADMIN')")` = 0 处 | **以 §7.10 第 2 条为准**，回写 §0.3/§0.4/§5 三处（与 G-03 合并执行） |

---

## 四、待用户决策问题批次（Q-101 ~ Q-112，共 12 个）

> 合并规则：同类项合并、一问只问一件事；能让设计师/技术负责人自行裁决的**不拿来问用户**（表内标注「已由 X 裁决」）。
> 排序：先「阻塞原则制定」→ 再「影响实现方案」→ 最后「只影响体验」。
> 编号自 Q-101 起（避开已用到的 Q-013 等）。**「不回也能继续吗」：可继续者按「我建议的默认项」先行实现并标注「未决先行，待复核」**。

### 第一批 · 阻塞原则制定（先问）

| Q | 问题原文（一问只问一件事） | 我建议的默认项 + 一句理由 | 不回也能继续吗 | 影响面 | 成本 |
|---|---|---|---|---|---|
| Q-101 | 「凡面向用户的筛选/展示维度，必须先在后台有录入入口且能真正写入数据库，否则不得放出该维度」——是否同意将这条立为**产品硬原则**（PR-01）？ | **同意**。本条是 P0-01（辣度筛选假功能）、P1-02（alias 死入口）的共同根因，立住它可一次性拦住后续所有「有维度无数据」问题。 | 是（我按「同意」推进，同时在原则文档标注待复核） | 三端所有筛选/展示维度 | S |
| Q-102 | 「同一业务口径（排序/分页/鉴权/错误码）的权威方固定为后端，端上只消费不覆写；文档与代码冲突时一律以最新拍板决议为准，不得据代码反推文档」——是否同意立为硬原则（PR-02）？ | **同意**。可同时终结 P0-06（端上覆写 sort）、P0-07（鉴权双口径）、P2-05（403 分歧）三类反复扯皮。 | 是（按「同意」推进） | 全部跨端口径 | S |

### 第二批 · 影响实现方案（次问）

| Q | 问题原文（一问只问一件事） | 我建议的默认项 + 一句理由 | 不回也能继续吗 | 影响面 | 成本 |
|---|---|---|---|---|---|
| Q-103 | 首页/搜索**是否还需要「品类（品类滚轮）」这个筛选维度**？ | **不做品类，删除死链路**（`api/category.ts` + store category 分支）。§7 四维（食堂/价格/辣度/热度）未含品类，符合「零消费即下线」。 | 是（按默认删死链，不影响主体） | 小程序首页/搜索 | S |
| Q-104 | **菜品详情页的评价是否开放「有用」点赞入口**（游客可点、点击弹认证），并让「按有用数置顶」真正生效？ | **开放 + 生效**（恢复按钮、去 `sort:'latest'` 覆写，对齐 §7.14 第 2 条 / §7.18 第 3 条）。若撤回该决议须一并下线接口，代价更大。 | 是（按默认实现，属落地已拍板决议） | 小程序详情页 + 后端排序 | M |
| Q-105 | 筛选行最右的**「筛选」胶囊（当前点击无反应）**是接上真实筛选面板，还是直接删除？ | **直接删除**，避免上线期出现假控件（PR-11）；排序不另设切换入口（与 §7.17 第 2 条「热度优先、不加排序入口」一致）。**已由 UI-UX 设计师裁决为「先移除」**，此处仅需确认。 | 是 | 小程序首页筛选行 | S |
| Q-106 | 工作台每次打开额外跑的**全表菜品聚合 + 十余条趋势查询**（前端不消费），是否先摘掉、只保留待办 + 规模指标 + 近期操作？ | **摘掉（懒计算或移除）**。§0.4.1 已定「不含图表看板」，无谓的全表扫描应清除。 | 是 | 后端 `/admin/dashboard` | M |
| Q-107 | **后台是否需要「实体审核（菜品/档口/食堂）」页面**（当前 `/admin/audit` 有接口零入口、pending 无来源）？ | **不新增页面，删除死代码**（`api/audit.ts` 三函数 + `auditToLegacy` + `AuditVO`），并把 `dish_audit` 通知登记为「仅存量兼容」——因管理员录入即 approved，pending 无新来源。**若线上仍存在 pending 存量，须先告知我改默认项。** | 是（按默认删死代码） | Web 后台 + 后端通知类型 | M |
| Q-108 | §5.y.1 已声明 `PUT /auth/password` **废弃移除**，但实现仍在（学生侧无密码体系、仅管理员语义）——**删除该端点，还是加 ADMIN 闸门并回写 spec**？ | **删除**，对齐已拍板的废除口径；学生端无密码体系，保留只会制造语义混乱。 | 是 | 后端鉴权 + spec §5.y.1 | S |
| Q-109 | 热度权重常量 `DishHeatWeights` 当前**无任何代码引用**（公式只活在 SQL）——**保留（作口径登记）还是删除**（避免误以为改了它生效）？ | **保留并补注释「当前无运行时引用，仅作口径登记」**。§7.13 第 4 条要求与 SQL 等价，保留常量可在改权重时提供双处同步锚点。 | 是 | 后端热度口径 | S |
| Q-110 | 评价被内容安检判为 `review`/`rejected`（对他端不可见）时，**其星级是否计入菜品平均分与评价数**？ | **保持计入**。评分是内容质量信号、安检是合规信号，二者解耦；若反向会使「评分人数」与他端可见评价数长期背离。 | 是 | 后端评分聚合 + 首页排序 | M |
| Q-111 | §7.7 第 1 条说 UGC 准入不满足统一返回 **403**，但代码拆成 `4031`（未认证，弹引导）/ `403`（已认证无 openid，仅 toast）——**是否同意保留代码分流并据此修订 §7.7 表述**？ | **同意保留分流并修订 §7.7**。拆分更合理、§3 已登记 4031，且前端体验更好。 | 是（按默认保留实现，待确认后修 §7.7） | 小程序准入 + spec §7.7 | S |

### 第三批 · 只影响体验（后问，可批量默认）

| Q | 问题原文（一问只问一件事） | 我建议的默认项 + 一句理由 | 不回也能继续吗 | 影响面 | 成本 |
|---|---|---|---|---|---|
| Q-112 | 一组后台体验项**是否需要**（合并为一次确认）：① 食堂/档口「改名/停业」编辑入口；② 编辑保存前「他人已修改」轻量提示；③ 删除菜品确认文案加「评价影响」说明；④ 反馈「标记处理」加二次确认；⑤ 操作日志按时间区间筛选？ | **全做**（① 补编辑入口不提供删除；② `updatedAt` 比对仅提示不阻塞；③ 加文案；④ 加二次确认；⑤ 补日期选择器）。均为低成本、堵运营与数据风险。 | 是（按默认全做） | Web 后台 | M |

> **已由其它角色裁决、不并列提问**：G-01/G-02 的落地方式（技术负责人 + 三端开发，按 §7 执行）；G-03/G-15 鉴权口径回写（技术负责人，按 §7.10 单向收敛）；G-07 表述精确化（技术负责人）；G-08 §7.4 表述修订（技术负责人）；P3-01 §4.9 标题红线（UI-UX 设计师，按红线执行）；Q-105 的「先移除死胶囊」已由 UI-UX 设计师裁决。

---

## 附 · 六份报告原始条目 → 合并映射（可追溯）

| 本文件条目 | 合并前来源 |
|---|---|
| P0-01 | BE-01 |
| P0-02 | TL-04 |
| P0-03 | WEB-01 |
| P0-04 | QA-01 + QA-15 |
| P0-05 | CL-03 + UX-03 |
| P0-06 | TL-01 + UX-01 + UX-02 + UX-04（排序入口相关） |
| P0-07 | TL-02 + TL-03 + BE-15 |
| P1-01 | WEB-03 + WEB-14 + BE-02 + TL-09 |
| P1-02 | WEB-02 |
| P1-03 | WEB-05 |
| P1-04 | WEB-04 |
| P1-05 | BE-13 |
| P1-06 | CL-04 + UX-07（+CL-05 失败态相关） |
| P2-01 | BE-03 + BE-04 + BE-05 + BE-07 |
| P2-02 | BE-06（+ §7.13 第 4 条） |
| P2-03 | BE-09 |
| P2-04 | BE-08 + BE-10 |
| P2-05 | TL-15 |
| P2-06 | BE-12（portion 部分）+ BE-14 |
| P2-07 | TL-05 + QA-02 |
| P2-08 | TL-06 + TL-10 + QA-07 |
| P2-09 | CL-01 + CL-10 + UX-04 + TL 缺失原则 #2 |
| P2-10 | TL-07 + QA-06 + WEB-11（部分）+ BE-15（悬空承诺） |
| P2-11 | CL-02 + CL-05 + CL-09 + CL-11 + CL-12 + CL-13 + CL-14 + CL-15 + QA-03 |
| P3-01 | CL-06 |
| P3-02 | BE-11 |
| P3-03 | QA-09 |
| P3-04 | QA-03 + CL-02 |
| P3-05 | QA-04 |
| P3-06 | QA-05 |
| P3-07 | WEB-11 |
| P3-08 | WEB-12 |
| P3-09 | WEB-08 |
| P3-10 | TL-08 + BE-03（枚举收敛） |
| P3-11 | TL-12 |
| P3-12 | BE-12（VO 部分） |
| P3-13 | CL-07 |
| P3-14 | CL-08 |
| P3-15 | WEB-07 |
| P3-16 | WEB-13 |
| P3-17 | QA-10 |
| P3-18 | TL-13 |
| P3-19 | TL-11 |
| P3-20 | TL-14 + WEB-06 + WEB-09 + WEB-10 + WEB-15 + QA-08 + QA-11 + QA-12 + QA-13 + QA-14 |
| 未计入 | UX-05/UX-06/UX-08/UX-09/UX-10/UX-11/UX-12 中「已由 §7 或设计侧裁决」的体验项，其中 UX-06/UX-12（详情页拆卡合并）归 UI-UX 设计师裁决；其余体验项随 P1-06/P3-20 的处置一并收敛 |
