# 浏览计数 — 页面 UI 设计稿

> 所属端：学生端（微信小程序） ｜ 归属功能文档：[client-浏览计数.md](../feature/client-浏览计数.md)
> **口径分工**：**页面 UI 设计口径以本文件为唯一真源**，功能流程 / 接口 / 字段 / 数据落库口径以功能文档为准。
> 落点：**无独立界面** —— 纯埋点（进菜品详情页时上报一次），无任何可见 UI；本文档记录「为什么没有界面」与埋点口径。


无独立界面（进菜品详情页自动触发，用户无感知）。

> **埋点口径（见功能文档）**：`POST /dishes/{id}/views` **每次进入页面即 +1**
> （PV 口径，不做人员 / 时间 / 自然日限制），端点**公开**（游客同样触发、UI 上无任何差异）；
> 唯一的服务端兜底是 **IP 维度限频**，正常浏览速率下**用户无感** —— 故本页仍**无任何可见 UI**。
