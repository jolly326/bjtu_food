# 反馈处理 — 页面 UI 设计稿

> 所属端：管理端（Web 后台） ｜ 归属功能文档：[web-反馈处理.md](../feature/web-反馈处理.md)
> **口径分工**：**页面 UI 设计口径以本文件为唯一真源**，功能流程 / 接口 / 字段 / 数据落库口径以功能文档为准。
> 落点：`/dashboard/feedback`（`FeedbackView`）；导航名为「**反馈**」——后台唯一运营闭环（采纳回复 / 不采纳必填原因）。


- 导航「反馈」→ `/dashboard/feedback`（导航项带**待处理徽标**，替代原工作台看板）。
- 筛选：状态（**默认待处理** / 已处理 / 全部）+ 类型 + 关键词搜索。
- 表格列：类型 / 关联对象 / 内容 / 提交人 / 时间 / 状态。
- 处理抽屉：左看内容与关联对象（含配图 ≤3 张），右写回复与结论。
